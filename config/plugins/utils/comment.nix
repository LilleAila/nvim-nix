{
  plugins.comment = {
    enable = true;
    settings = {
      padding = true;
      sticky = true; # Keep cursor at position
      toggler = {
        line = "gcc";
        block = "gbc";
      };
      opleader = {
        line = "gc";
        block = "gb";
      };
      mappings.basic = true;
    };
  };
}
